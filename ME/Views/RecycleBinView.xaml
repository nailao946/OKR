<UserControl x:Class="ME.Views.RecycleBinView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel>
            <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="已删除目标" FontSize="16" FontWeight="Bold"
                               Foreground="{StaticResource TextBrush}" Margin="0,0,0,12" />
                    <ItemsControl ItemsSource="{Binding DeletedGoals}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{StaticResource BackgroundBrush}" CornerRadius="8"
                                        Padding="12" Margin="0,0,0,6">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding Name}" FontSize="13" Foreground="{StaticResource TextBrush}" />
                                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                                            <Button Content="恢复" Command="{Binding DataContext.RestoreGoalCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}" Style="{StaticResource PrimaryButtonStyle}"
                                                    Padding="8,4" Margin="0,0,4,0" FontSize="11" />
                                            <Button Content="彻底删除" Command="{Binding DataContext.PermanentlyDeleteGoalCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}" Style="{StaticResource SecondaryButtonStyle}"
                                                    Padding="8,4" FontSize="11" />
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </DockPanel>
            </Border>

            <Border Style="{StaticResource CardStyle}">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="已删除任务" FontSize="16" FontWeight="Bold"
                               Foreground="{StaticResource TextBrush}" Margin="0,0,0,12" />
                    <ItemsControl ItemsSource="{Binding DeletedTasks}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{StaticResource BackgroundBrush}" CornerRadius="8"
                                        Padding="12" Margin="0,0,0,6">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding Title}" FontSize="13" Foreground="{StaticResource TextBrush}" />
                                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                                            <Button Content="恢复" Command="{Binding DataContext.RestoreTaskCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}" Style="{StaticResource PrimaryButtonStyle}"
                                                    Padding="8,4" Margin="0,0,4,0" FontSize="11" />
                                            <Button Content="彻底删除" Command="{Binding DataContext.PermanentlyDeleteTaskCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}" Style="{StaticResource SecondaryButtonStyle}"
                                                    Padding="8,4" FontSize="11" />
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </DockPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
