<UserControl x:Class="ME.Views.MapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             IsVisibleChanged="MapView_IsVisibleChanged">
    <Border Style="{StaticResource CardStyle}">
        <DockPanel>
            <TextBlock DockPanel.Dock="Top" Text="目标地图 - 全局规划" FontSize="16" FontWeight="Bold"
                       Foreground="{StaticResource TextBrush}" Margin="0,0,0,16" />
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                <TreeView x:Name="GoalTree" Background="Transparent" BorderThickness="0"
                          Foreground="{StaticResource TextBrush}">
                    <TreeView.Resources>
                        <Style TargetType="TreeViewItem">
                            <Setter Property="FontSize" Value="13" />
                            <Setter Property="Padding" Value="6,4" />
                            <Setter Property="IsExpanded" Value="True" />
                        </Style>
                    </TreeView.Resources>
                </TreeView>
            </ScrollViewer>
        </DockPanel>
    </Border>
</UserControl>
